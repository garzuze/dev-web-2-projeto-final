import { Routes } from '@angular/router';

// Cada feature registra suas rotas aqui usando loadComponent / loadChildren,
// para que os bundles fiquem separados e duas pessoas nao editem o mesmo
// arquivo de rotas o tempo todo.
export const routes: Routes = [];
